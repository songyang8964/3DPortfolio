import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css' ;
// React will get into the root and create it
ReactDOM.createRoot(document.getElementById('root')).render(<App />)